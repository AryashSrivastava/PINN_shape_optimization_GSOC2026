# PINN_shape_optimization_GSOC2026
This project, is a demonstration of Physics-Informed Neural Networks (PINNs) applied to computational physics and fluid dynamics problems.  By leveraging the PyTorch framework, it illustrates how neural networks can be trained to solve partial differential equations (PDEs) by embedding the underlying physical laws directly into the loss function.

### Results Analysis has been done in the code itself.
